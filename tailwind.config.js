tailwind.config = {
  darkMode: "class",
  theme: {
    extend: {
      colors: {
        // Standardize dark theme color
        darkTheme: "#121826",
      },
      // Replace current fonts with modern, readable alternatives
      fontFamily: {
        montserrat: ["Montserrat", "sans-serif"],
        roboto: ["Roboto", "sans-serif"],
      },
    },
  },
};
